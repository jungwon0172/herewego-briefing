# Vercel KV 연결 (Vercel 대시보드 > Storage > KV 생성 시 자동 발급됨)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# 수집 파이프라인(자동 검색 후 draft 등록)이 /api/news POST 호출할 때 쓰는 비밀키
COLLECTOR_SECRET=

# 관리자 페이지(/admin)에서 승인/거절할 때 쓰는 비밀키 (비밀번호처럼 사용)
ADMIN_SECRET=
